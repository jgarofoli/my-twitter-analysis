
cat(" you must fill this in not-privateRdata.R ")

if (username == "somename1") {
    api_key             <- ""
    api_secret          <- ""
    access_token        <- ""
    access_token_secret <- ""
} else if (username == "somename2") {
    api_key             <- ""
    api_secret          <- ""
    access_token        <- ""
    access_token_secret <- ""
}
